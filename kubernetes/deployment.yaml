apiVersion: apps/v1
kind: Deployment
metadata:
  name: contact-form-main-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: contact-form-main-app
  template:
    metadata:
      labels:
        app: contact-form-main-app
    spec:
      containers:
        - name: contact-form-main-app
          image: contact-form-main-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /ContactForm/
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
